# Default local backend URL
VITE_API_URL=http://127.0.0.1:8000

# Colab tunnel URL changes every runtime/session.
# Copy from Colab Step 4 and replace the value above.
# VITE_API_URL=https://example-subdomain.trycloudflare.com

# Optional Gemini-backed RAG settings for the Python backend.
# GOOGLE_API_KEY=your_real_key_here
# RAG_LLM_PROVIDER=gemini
