# MultiAgent Backend Requirements (No Ollama)
# ===========================================
# Core API
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9

# OCR and image processing
opencv-python>=4.9.0
Pillow>=10.0.0
pytesseract>=0.3.10

# ML stack (resource-aware)
numpy>=1.26.0
scikit-learn>=1.4.0

# Utilities
requests>=2.31.0
python-dotenv>=1.0.0

# PostgreSQL persistence
sqlalchemy>=2.0.0
psycopg[binary]>=3.1.0
alembic>=1.13.0

# Optional extras (install separately if needed in your runtime):
#   pip install easyocr
#   pip install langchain langchain-community langchain-text-splitters chromadb sentence-transformers pypdf
#   pip install langchain-google-genai google-generativeai